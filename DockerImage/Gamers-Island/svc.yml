apiVersion: v1
kind: Service

metadata:
  name: game-svc
  namespace: karni2407

spec:
  type: NodePort

  selector:
    app: gamers-island

  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
