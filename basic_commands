before port-forward, use sudo firewall-cmd --add-port=<port>/<protocol> (8000/tcp)
kubectl port-forward svc/my-service --address=0.0.0.0 8000:80 (host port:container port)
,host port is up to you , container port must be changed before making the docker image or maybe --bind command
command for interactive shell - kubectl exec -it exam-portal-84644b58f8-68fk6 -n karni2407 -- /bin/bash
which is basically for accessing pods to provide whatever changes may be required

rolling updates - kubectl set image deployment/redis-deployment -n redis-namespace redis-container=redis:8.0
